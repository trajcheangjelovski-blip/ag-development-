# Caddy auto-provisions and renews a free Let's Encrypt TLS certificate.
# DNS: point both A records (ag-development.dev and www) at your server's IP.

ag-development.dev {
	reverse_proxy app:3000
}

www.ag-development.dev {
	redir https://ag-development.dev{uri} permanent
}
